#include<stdio.h>

int main()
{
    // Variable declaration
    int h, w, d, vol;
    // Assign value the variables
    h= 10;
    w= 12;
    d= 8;
    // Calculate using mathematical formula
    vol= h*w*d;
    // Display the volume
    printf("The volume is the cube is: %d", vol);
    // End the main program
    return 0;
}
